
todo
X - reimpl items crud w/ new types & helpers
X - tag crud action resp with `Action` & use in client
X - reimpl subs
X - basic reconnect
X - conn helpers
  X * wrap ws conn (client?) in lustre app?
  X * helper to detect dis/connected `Bool`
X - refactor
  X * separate `api/client.ApiClient` (javascript target) from `app/api.Api` (shared api def)
    X - make generic w/o ws impl
    X - make javascript impl
  X * unify `ApiClient` with `Conn` (ws reconnect logic)
X - reconnect detection
  X * track reconnect client-side
  - handle `todo`s in err paths (and use errs instead of `Result(t, Nil)`)

clean up
  - mv generic logic from `client` & `app` into `api`
  - separate generic from future-codegen logic
  - clean up
    * dead code, comments
    * duped types, e.g. `SocketResp`, `Err`
    * tk

next
  - impl unsubscribe
  - send msg to client to notify un/successful subscription

maybe
  - conv `FuncReq` to plain `fn(param) -> Result(return, Err)`
  - configurable timeout callbacks for hung reqs
